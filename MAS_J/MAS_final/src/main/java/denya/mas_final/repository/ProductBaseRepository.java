package denya.mas_final.repository;

import denya.mas_final.model.ProductBase;
import org.springframework.data.repository.CrudRepository;

public interface ProductBaseRepository extends CrudRepository<ProductBase, Long> {
}
